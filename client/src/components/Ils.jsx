/** Israeli New Shekel sign in Bitcount Ink (font-serif). */
export default function Ils({ className = "" }) {
  return (
    <span className={`font-serif ${className}`.trim()} aria-hidden="true">
      ₪
    </span>
  );
}
